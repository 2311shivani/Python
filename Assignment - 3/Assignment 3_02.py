#2.Write a program which accept N numbers from user and store it into List. Return Maximum number from that List.

def Number():
    list = []

No = int(input(" Enter number of element in list: "))

for i in range(1,No + 1):
     int(input(" Enter the Element: "))
list(int)

print("Maximum element is:",Number(No + 1))
